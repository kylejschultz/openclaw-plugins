{
  "id": "media-commands",
  "name": "Media Commands",
  "description": "Persistent Discord media stack control panel.",
  "version": "0.5.2",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "panelChannelId": {
        "type": "string",
        "description": "Discord channel id for the resident media panel message."
      },
      "panelMessageId": {
        "type": "string",
        "description": "Optional fixed Discord message id to edit instead of using plugin state."
      },
      "accountId": {
        "type": "string",
        "description": "Optional Discord account id."
      },
      "mediaMcpUrl": {
        "type": "string",
        "description": "Media MCP Streamable HTTP URL used by panel-triggered MVP actions."
      },
      "autoPost": {
        "type": "boolean",
        "description": "Post or repair the resident panel on plugin startup."
      }
    }
  },
  "activation": {
    "onStartup": true
  },
  "contracts": {}
}
